using UnityEngine;

[RequireComponent(typeof(PlayerProximityInteractor))]
public class PlayerRayInteractor : PlayerProximityInteractor
{
    // Deprecated placeholder to keep existing scene references working.
}
